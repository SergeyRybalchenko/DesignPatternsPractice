namespace AdapterPattern
{
    // Необходимо реализовать интерфейс, реализация уже имеется, но не соответствует интерфейсу
    public interface ISender
    {
        void Send(string message);
    }
}
